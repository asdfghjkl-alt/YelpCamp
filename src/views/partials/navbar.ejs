<nav class="navbar sticky-top navbar-dark navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/campgrounds"
            ><img src="/images/navbar-icon.png" class="navbar-icon" />
            Yelpcamp</a
        >
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/campgrounds"
                        >Home</a
                    >
                </li>
                <% if (currentUser) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/campgrounds/new"
                        >New Campground</a
                    >
                </li>
                <% } %>
            </ul>
        </div>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <% if (!currentUser) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/auth/register">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/auth/login">Login</a>
                </li>
                <% } else { %>
                <div class="d-flex align-items-center">
                    <li class="nav-item me-2 display-name">
                        <span class="nav-text">
                            Welcome <%= currentUser.username %>
                        </span>
                    </li>
                    <form action="/auth/logout" method="POST">
                        <button class="btn btn-outline-secondary">
                            Logout
                        </button>
                    </form>
                </div>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
